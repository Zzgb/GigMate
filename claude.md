# CLAUDE.md - GigMate 兼职平台  ## 项目概述 GigMate 是一个连接雇主与自由职业者的短期兼职平台。用户分为雇主（EMPLOYER）和自由职业者（FREELANCER）两种角色。  ## 技术栈 - **框架**: Next.js 16 (App Router) + TypeScript - **样式**: Tailwind CSS 4 + shadcn/ui (Nova 预设, Base 组件库) - **数据库**: PostgreSQL (Neon 托管) - **ORM**: Prisma 7 - **包管理器**: pnpm  ## 启动命令 | 命令 | 用途 | |------|------| | `pnpm dev` | 启动开发服务器 (localhost:3000) | | `pnpm build` | 生产构建 | | `pnpm lint` | 代码检查 | | `npx prisma db push` | 推送 schema 变更到数据库 | | `npx prisma db studio` | 打开数据库可视化管理 |  ## 目录结构 src/ ├── app/ # 页面路由 (App Router) │ ├── layout.tsx # 根布局 │ ├── page.tsx # 首页 │ ├── (auth)/ # 登录/注册 (无需登录) │ └── (dashboard)/ # 需登录页面 │ ├── employer/ # 雇主: 发布任务、管理申请 │ └── freelancer/ # 自由职业者: 浏览任务、我的申请 ├── components/ # 共享 UI 组件 │ └── ui/ # shadcn 组件 (不可手动改) └── lib/ # 工具函数 (auth, db, utils)   ## 数据模型 (prisma/schema.prisma) - **User**: id, email, name, role (EMPLOYER/FREELANCER) - **Task**: id, title, description, budget, deadline, status (OPEN/IN_PROGRESS/COMPLETED), category, skills[], employerId - **Application**: id, message, status (PENDING/ACCEPTED/REJECTED), taskId, freelancerId - **Review**: id, rating, comment, taskId, reviewerId, revieweeId  ## 核心业务规则 - 任务状态流转: OPEN → IN_PROGRESS → COMPLETED - 只有 OPEN 状态的任务可申请 - 一个任务只能有一个被接受的申请 - 任务完成后双方互评 - 不同角色看到不同操作按钮和页面  ## 文件约定 - 页面组件用默认导出 - 服务端组件直接调 Prisma，客户端组件通过 API 路由获取数据 - 所有 API 路由做权限验证 - 金额字段以美元为单位，保留两位小数，显示时加 `$` 前缀